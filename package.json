{
  "name": "contractguard-ai-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Local MVP demo for ContractGuard AI contract risk review.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --check server.js && python3 -m py_compile backend/analyze_contract.py"
  },
  "engines": {
    "node": ">=18"
  }
}
